---
kind: pipeline
type: docker
name: Run Tests

platform:
  os: linux
  arch: arm

steps:
  - name: Build
    image: haxe:4.2.4
    commands:
      - haxelib install --always build.hxml
      - haxe build.hxml
  - name: Composer install
    image: composer
    commands:
      - composer install
  - name: Run Tests on PHP7
    image: php:7-cli-alpine
    depends_on:
      - Composer install
      - Build
    commands:
      - php ./bin/tests/index.php
  - name: Run Tests on PHP8
    image: php:8-cli-alpine
    depends_on:
      - Composer install
      - Build
    commands:
      - php ./bin/tests/index.php

---
kind: pipeline
name: Release

platform:
  os: linux
  arch: arm

steps:
  - name: Zip Library
    image: alpine:3.15.2
    commands:
      - apk add zip jq
      - cat haxelib.json | jq -r ". += {\"version\":\"${DRONE_TAG}\"}" > tmp && mv tmp haxelib.json
      - zip -r library.zip src *.md haxelib.json README
  - name: Haxelib Publish
    image: haxe:4.2.4
    commands:
      - haxelib submit library.zip $HAXELIB_PASSWORD --always
    environment:
      HAXELIB_PASSWORD:
        from_secret: HAXELIB_PASSWORD

trigger:
  event:
  - tag

depends_on:
- Run Tests
---
kind: signature
hmac: 1f0792e6547d55ac4ee99b5a67280d4db898b41a6a8d0ebfd2f9b25b1cea4873

...
